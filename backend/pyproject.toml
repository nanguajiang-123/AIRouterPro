[project]
name = "backend"
version = "0.1.0"
description = "SDN Control Platform — FastAPI backend"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.136.3",
    "loguru>=0.7.3",
    "uvicorn>=0.49.0",
]
